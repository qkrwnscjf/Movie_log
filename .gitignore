# Dependency directories
node_modules/
/dist
/.next/
/out/
/build

# Environment variables (보안 필수! 실 서비스 키 유출 방지)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local
*.env

# OS Metadata (운영체제 생성 임시 파일)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Debug logs (개발 중 발생하는 로그 파일)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs
*.log

# Vercel (로컬 프로젝트 연결 정보 및 설정)
.vercel
.vercel/project.json

# TypeScript / Build Info (빌드 속도 및 캐시 관련)
*.tsbuildinfo
next-env.d.ts
.turbo

# IDE Settings (개별 개발 환경 설정 파일)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Security & Credentials (인증서 및 비밀키)
*.pem
*.key
*.cert
*.crt

# Lint & Format Cache (도구별 캐시 파일)
.eslintcache
.stylelintcache
.prettiercache
